# build artifacts
/bethoven
/bethoven-linux-amd64
/bethoven-linux-arm64
/bin/

# runtime state (never commit these)
*.db
*.db-wal
*.db-shm
*.db.bak
host_key
data/

# SSH private keys must never be committed
id_ed25519
id_ed25519.pub
id_rsa
id_rsa.pub
*.pem

# personal deploy script (contains invite code + admin fingerprint)
deploy/local-deploy.sh
bethoven-linux-amd64

# BETanIA pick log (durable history; not source)
ai_bets.log
